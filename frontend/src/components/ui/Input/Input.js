import React from "react";

/*
function Input(...props) {
  return (
    <input
                  type="text"
                  className="form-control rounded-pill form-control-lg dashboard__btn"
                  value={email}
                  onChange={props.handleOnChange}
                  placeholder={props}
                  "E-mail Address"
                />
  )
}

export default Input*/
